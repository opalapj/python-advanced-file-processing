[DEFAULT]
name = Piotr

[Paths]
name = Karolina
home_dir: /Users
my_dir: %(home_dir)s/lumberjack
my_pictures: %(my_dir)s/Pictures
# Refering to other values in the same section (primary), or values in the special default section (secondary). Comment option ('#') to check.
interpolation = %(name)s

[Escape]
# use a %% to escape the % sign (% is the only character that needs to be escaped):
gain: 80%%